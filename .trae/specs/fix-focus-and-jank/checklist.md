# Checklist

- [x] 首页"最近文章"点击 → 跳 `/posts` 列表并滚动定位+高亮该文章（焦点提示恢复，实测 is-focused 高亮 3s 后消失）
- [x] posts 列表点卡片 → 单篇；深链接 `/posts/<id>` → 单篇（行为不变，实测通过）
- [x] about 页吊牌观感无回归（物理步频 1/30、分段 16、静止跳过重建后正常渲染）
- [x] projects/about 页波浪背景静态化但保持像素观感（实测两帧 diff=0 但画面非空白），其他页动画正常
- [x] about 页性能复测：FPS 111.8 → 115.6（环境校正约 +8%）；帧间隔 p95 12.6ms → 4ms；长任务 1×138ms → 0；渲染循环冻结后主线程/GPU 负载释放
- [x] projects 页不掉帧（130.6 vs 基线 134.3，同机 home 同降背景下持平）
- [x] 全站浏览器回归：3D 星图/吊牌/波浪/动效/终端/登录/评论/点赞正常，控制台无 error（A1-A7 全部通过）
- [x] `npm run build` 通过
